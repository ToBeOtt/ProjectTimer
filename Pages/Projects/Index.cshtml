@page
@model ProjectTimer.Pages.Projects.IndexModel
@{
}
<link rel="stylesheet" href="~/css/projectpages.css" asp-append-version="true" />

<div class="container px-4">
  <div class="row gx-5">
    <div class="col">
     <div class="p-3" id="leftGutter">
                <h4>Lägg till projekt</h4>
                <form method="post">
                    <div class="mb-3" id="rightGutter">
                        <label for="formGroupExampleInput" class="form-label"></label>
                        <input type="text" class="form-control" value="" asp-for="Name">
                    </div>
                    <div class="mb-3">
                        <button type="submit" class="btn btn-outline-success">Lägg till projekt</button>
                    </div>
                </form>
            @if (Model.projectList != null)
            {
                    <h5 style="text-align: center;">Dina befintliga projekt</h5>
                    <br />
                    <ul class="nav" id="ul-show-project">
                            @foreach (var project in Model.projectList)
                            {
                                <li class="nav-item">
                                    <div class="mb-3">
                                    <button type="submit" class="btn btn-outline-success" name="pID" value="@project.Id">
                                            @project.Name
                                        </button>
                                        <img type="submit" src="~/images/trashcan.svg" alt="Picture of Trashcan" name="projectDelete" value="@project.Id">
                                        <img type="submit" src="~/images/editpen.svg" alt="Picture of pencil" name="projectEdit" value="@project.Id">
                                    </div>
                                </li>
                                <br />
                                }
                        </ul>
                    @if(Model.EditTest)
                    {
                        <p>Edit funkar</p>
                        
                    }

                }   
     </div>
    </div>
    <div class="col">
            <h5>Här visas sparad tid från olika projekt</h5>
            <p>Med projekt i olika grupper tydligt avgränsade. Varje project går att visa alla sparade tider. 
                Dessa kan sedan summeras ihop allihopa eller var för sig. Man kan också radera manuellt ett inlägg manuellt.
            </p>
            <div class="mb-3">
                <button type="submit" class="btn btn-outline-success">Spara odebiterad projekttid</button>
            </div>
    </div>
        
    </div>
</div>







                     @*<li class="nav-item">
                                <div class="mb-3" id="content-show-project">
                                    @project.Name
                                        <form method="post" asp-page-handler="DeleteProject">
                                            <img type="submit" class="alter-object-svgs" src="~/images/trashcan.svg"  alt="Picture of Trashcan" name="projectDelete" value="@project.Id">
                                         </form>
                                         <form method="post" asp-page-handler="EditProject">
                                            <img type="submit" class="alter-object-svgs" src="~/images/editpen.svg"  alt="Picture of pencil" name="projectEdit" value="@project.Id">
                                        </form>
                                    </div>
                                </li>*@